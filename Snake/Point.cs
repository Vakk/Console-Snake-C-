using System;

namespace Snake
{	/**
	 * point, base class for all elements, has position*/
	public class Point
	{
		protected int x;
		protected int y;
		public Point (int x,int y)
		{
			this.x = x;
			this.y = y;
		}
	}
}

